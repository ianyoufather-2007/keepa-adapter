{
  "name": "keepa-adapter",
  "version": "1.1.1",
  "description": "SkillCrate adapter: MCP server + OpenClaw skill for Keepa Amazon product monitoring",
  "type": "module",
  "bin": {
    "keepa-adapter": "./dist/index.js"
  },
  "exports": {
    ".": "./dist/openclaw.js",
    "./client": "./dist/adapter/client.js",
    "./endpoints": "./dist/adapter/endpoints.js",
    "./transformer": "./dist/adapter/transformer.js",
    "./schema": "./dist/schema/universal.js",
    "./constants": "./dist/constants.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "mcpb:stage": "node scripts/prepare-mcpb.mjs",
    "mcpb:validate": "npm run build && npm run mcpb:stage && mcpb validate .mcpb-build",
    "mcpb:pack": "npm run build && npm run mcpb:stage && mcpb validate .mcpb-build && node scripts/pack-mcpb.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "discover": "tsx scripts/discover.ts",
    "collect": "tsx src/scheduler/runner.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "sql.js": "^1.14.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^22.15.3",
    "@types/sql.js": "^1.4.11",
    "tsup": "^8.4.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
